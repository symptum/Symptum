<ContentDialog
    x:Class="Symptum.Editor.Controls.QuestionBankContextConfigureDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Symptum.Editor.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{ThemeResource DefaultContentDialogStyle}"
    Title="Configure Question Bank Context"
    PrimaryButtonText="Update" PrimaryButtonStyle="{ThemeResource AccentButtonStyle}"
    SecondaryButtonText="Cancel">
	<StackPanel Spacing="10" Width="300">
    <ComboBox x:Name="scCB" Header="Subject Code" HorizontalAlignment="Stretch" />
    <DatePicker x:Name="datePicker" Header="Last Input Date" DayVisible="False" HorizontalAlignment="Stretch" />
    <TextBlock Text="Preferred Book" />
    <local:BookReferencePicker x:Name="bookRefPicker" HorizontalAlignment="Stretch" />
  </StackPanel>
</ContentDialog>
