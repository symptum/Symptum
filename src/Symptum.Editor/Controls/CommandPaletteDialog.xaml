<ContentDialog
  x:Class="Symptum.Editor.Controls.CommandPaletteDialog"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:local="using:Symptum.Editor.Controls"
  xmlns:cmd="using:Symptum.Editor.Commands"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  Style="{ThemeResource DefaultContentDialogStyle}"
  PrimaryButtonStyle="{ThemeResource AccentButtonStyle}"
  PrimaryButtonText="Execute" CloseButtonText="Cancel"
  DefaultButton="Primary">
  <Grid RowDefinitions="Auto,Auto">
    <AutoSuggestBox x:Name="commandBox" DisplayMemberPath="Key" />
    <ListView x:Name="optionsLV" Grid.Row="1" Margin="0,16,0,0" SelectionMode="Single" MaxHeight="200">
      <ListView.ItemTemplate>
        <DataTemplate x:DataType="cmd:EditorCommandOption">
          <ListViewItem ToolTipService.ToolTip="{x:Bind Title}">
            <TextBlock Text="{x:Bind Title}" HorizontalAlignment="Stretch" VerticalAlignment="Center" />
          </ListViewItem>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>
</ContentDialog>
