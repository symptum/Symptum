<Page
    x:Class="Symptum.Pages.SubjectsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Symptum.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Symptum.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

  <ScrollViewer>
    <ItemsRepeater Margin="10" x:Name="subjectsView" ItemsSource="{x:Bind vm:MainViewModel.Subjects}">
      <ItemsRepeater.Layout>
        <UniformGridLayout MinColumnSpacing="10" MinRowSpacing="10" />
      </ItemsRepeater.Layout>
      <ItemsRepeater.ItemTemplate>
        <DataTemplate x:DataType="vm:SubjectInfo">
          <Button Width="150" Height="100" Click="Button_Click" Tag="{Binding Path}">
            <TextBlock Text="{Binding Name}" Style="{ThemeResource BodyStrongTextBlockStyle}" />
          </Button>
        </DataTemplate>
      </ItemsRepeater.ItemTemplate>
    </ItemsRepeater>
  </ScrollViewer>
</Page>
